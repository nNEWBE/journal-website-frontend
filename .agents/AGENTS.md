# Project Rules & Style Guidelines

- **Iconography**: Never use the `Sparkles` icon from `lucide-react`. Use academic icons like `BookOpen`, `Send`, `FileText`, `Library`, `Award`, or `ShieldCheck` instead.
